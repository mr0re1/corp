// Generated by CoffeeScript 1.6.3
(function() {
  var Emitter;

  Emitter = (function() {
    function Emitter() {
      this.listeners = {};
    }

    Emitter.prototype.on = function(event, listener) {
      if (this.listeners[event] == null) {
        this.listeners[event] = [];
      }
      return this.listeners[event].push(listener);
    };

    Emitter.prototype.off = function(event, listener) {
      var found, i, lst, _i, _j, _len, _ref;
      if (this.listeners[event] == null) {
        return false;
      }
      lst = this.listeners[event];
      found = [];
      for (i = _i = _ref = lst.length - 1; _ref <= 0 ? _i <= 0 : _i >= 0; i = _ref <= 0 ? ++_i : --_i) {
        if (lst[i] === listener) {
          found.push(i);
        }
      }
      if (!found.length) {
        return false;
      }
      for (_j = 0, _len = found.length; _j < _len; _j++) {
        i = found[_j];
        lst.splice(i, 1);
      }
      return true;
    };

    Emitter.prototype.emit = function(event, data) {
      var listener, _i, _len, _ref, _results;
      if (this.listeners[event] == null) {
        return;
      }
      _ref = this.listeners[event];
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        listener = _ref[_i];
        _results.push(listener(data));
      }
      return _results;
    };

    return Emitter;

  })();

  window.Emitter = Emitter;

}).call(this);
