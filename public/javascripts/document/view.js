// Generated by CoffeeScript 1.6.3
(function() {
  var DocumentView;

  DocumentView = (function() {
    function DocumentView(opt) {
      this.opt = opt;
      if (this.opt == null) {
        this.opt = {};
      }
    }

    DocumentView.prototype.show = function(document, $place) {
      var it, res, _i, _len, _ref;
      $place.empty();
      if (!document.content) {
        return;
      }
      res = [];
      _ref = document.content;
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        it = _ref[_i];
        if (typeof it === 'string') {
          res.push("<span class='plain'>" + it + "</span>");
          continue;
        }
        if ((it.type == null) || (JST["templates/doc/" + it.type] == null)) {
          console.error("Undefined item: ", it);
          continue;
        }
        res.push(JST["templates/doc/" + it.type](it));
      }
      return $place.html(res.join(''));
    };

    return DocumentView;

  })();

  window.DocumentView = DocumentView;

}).call(this);
