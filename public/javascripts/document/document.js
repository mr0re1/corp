// Generated by CoffeeScript 1.6.3
(function() {
  var Document,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  Document = (function(_super) {
    __extends(Document, _super);

    function Document(doc) {
      var key, value;
      Document.__super__.constructor.call(this);
      if (doc != null) {
        for (key in doc) {
          value = doc[key];
          this[key] = value;
        }
      } else {
        console.log('New document construct');
      }
    }

    Document.prototype.setContent = function(content) {
      if (content === this.content) {
        return true;
      }
      this.content = content;
      return this.emit('contentchange', content);
    };

    Document.prototype.setMeta = function(meta) {
      if (meta === this.meta) {
        return true;
      }
      this.meta = meta;
      return this.emit('metachange', meta);
    };

    Document.prototype.save = function() {
      return console.log('Try to save');
    };

    return Document;

  })(Emitter);

  window.Document = Document;

}).call(this);
